<Window x:Class="HDRGammaController.DashboardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:HDRGammaController"
        xmlns:vm="clr-namespace:HDRGammaController.ViewModels"
        Title="Gloam - Dashboard"
        Height="Auto" Width="1000"
        SizeToContent="Height"
        MaxHeight="900" 
        MinWidth="800" MinHeight="600"
        WindowStartupLocation="CenterScreen"
        Background="Transparent"
        Foreground="White"
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="CanResizeWithGrip">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <!-- Reusing styles from SettingsWindow for consistency -->
        <SolidColorBrush x:Key="AccentBrush" Color="#0078D4"/>
        <SolidColorBrush x:Key="CardBackground" Color="#2D2D2D"/>
        <SolidColorBrush x:Key="CardBorder" Color="#3F3F3F"/>
        <SolidColorBrush x:Key="TextDim" Color="#888888"/>
        
        <Style x:Key="MonitorCard" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource CardBackground}"/>
            <Setter Property="BorderBrush" Value="{StaticResource CardBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="Margin" Value="0,0,12,12"/>
            <Setter Property="Width" Value="320"/>
            <Setter Property="Height" Value="240"/>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Dark checkbox: default WPF chrome renders a light box that clashes with the theme -->
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal" Background="Transparent">
                            <Border x:Name="Box" Width="16" Height="16" CornerRadius="3"
                                    Background="#3D3D3D" BorderBrush="#666666" BorderThickness="1"
                                    VerticalAlignment="Center">
                                <Path x:Name="Check" Data="M3,8 L7,12 L13,4"
                                      Stroke="White" StrokeThickness="2"
                                      Visibility="Collapsed"/>
                            </Border>
                            <ContentPresenter Margin="6,0,0,0" VerticalAlignment="Center" RecognizesAccessKey="True"/>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Box" Property="Background" Value="{StaticResource AccentBrush}"/>
                                <Setter TargetName="Box" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                                <Setter TargetName="Check" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Box" Property="BorderBrush" Value="#999999"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Toggle Style (Copied for consistency) -->
        <Style x:Key="ModernToggle" TargetType="CheckBox">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Margin" Value="0,4"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
        
        <!-- Input Style -->
        <Style TargetType="TextBox">
             <Setter Property="Background" Value="#3D3D3D"/>
             <Setter Property="Foreground" Value="White"/>
             <Setter Property="BorderBrush" Value="{StaticResource CardBorder}"/>
             <Setter Property="BorderThickness" Value="1"/>
             <Setter Property="Padding" Value="8,6"/>
             <Setter Property="CaretBrush" Value="White"/>
        </Style>
        
        <Style TargetType="ComboBox">
             <Setter Property="Background" Value="#3D3D3D"/>
             <Setter Property="Foreground" Value="White"/>
             <Setter Property="BorderBrush" Value="{StaticResource CardBorder}"/>
             <Setter Property="BorderThickness" Value="1"/>
             <Setter Property="Padding" Value="6"/>
             <Setter Property="Margin" Value="0,0,8,0"/>
             <!-- Full template override: the default chrome draws a light dropdown popup
                  and a light toggle field regardless of Background, so restyling only the
                  items left the list bright white. -->
             <Setter Property="Template">
                 <Setter.Value>
                     <ControlTemplate TargetType="ComboBox">
                         <Grid>
                             <Grid.ColumnDefinitions>
                                 <ColumnDefinition Width="*"/>
                                 <ColumnDefinition Width="22"/>
                             </Grid.ColumnDefinitions>

                             <Border Grid.ColumnSpan="2"
                                     Background="{TemplateBinding Background}"
                                     BorderBrush="{TemplateBinding BorderBrush}"
                                     BorderThickness="{TemplateBinding BorderThickness}"
                                     CornerRadius="4"/>

                             <ToggleButton Grid.ColumnSpan="2" Focusable="False" ClickMode="Press"
                                           IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                 <ToggleButton.Template>
                                     <ControlTemplate TargetType="ToggleButton">
                                         <Border Background="Transparent">
                                             <Path HorizontalAlignment="Right" VerticalAlignment="Center"
                                                   Margin="0,0,8,0" Data="M0,0 L4,4 L8,0"
                                                   Stroke="#AAAAAA" StrokeThickness="1.5"/>
                                         </Border>
                                     </ControlTemplate>
                                 </ToggleButton.Template>
                             </ToggleButton>

                             <ContentPresenter x:Name="ContentSite" Grid.Column="0" IsHitTestVisible="False"
                                               Content="{TemplateBinding SelectionBoxItem}"
                                               ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                               Margin="{TemplateBinding Padding}"
                                               VerticalAlignment="Center" HorizontalAlignment="Left"/>

                             <TextBox x:Name="PART_EditableTextBox" Grid.Column="0" Visibility="Hidden"
                                      Margin="{TemplateBinding Padding}" Padding="0" BorderThickness="0"
                                      Background="Transparent" Foreground="White" CaretBrush="White"
                                      VerticalAlignment="Center" HorizontalAlignment="Stretch"
                                      Focusable="True" IsReadOnly="{TemplateBinding IsReadOnly}"/>

                             <Popup x:Name="PART_Popup" Grid.ColumnSpan="2" Placement="Bottom"
                                    IsOpen="{TemplateBinding IsDropDownOpen}"
                                    AllowsTransparency="True" Focusable="False" PopupAnimation="Fade">
                                 <Border MaxHeight="{TemplateBinding MaxDropDownHeight}"
                                         MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}"
                                         Background="#2D2D2D"
                                         BorderBrush="{StaticResource CardBorder}" BorderThickness="1"
                                         CornerRadius="4" Margin="0,2,0,0">
                                     <ScrollViewer VerticalScrollBarVisibility="Auto">
                                         <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained"/>
                                     </ScrollViewer>
                                 </Border>
                             </Popup>
                         </Grid>
                         <ControlTemplate.Triggers>
                             <Trigger Property="IsEditable" Value="True">
                                 <Setter TargetName="PART_EditableTextBox" Property="Visibility" Value="Visible"/>
                                 <Setter TargetName="ContentSite" Property="Visibility" Value="Hidden"/>
                             </Trigger>
                         </ControlTemplate.Triggers>
                     </ControlTemplate>
                 </Setter.Value>
             </Setter>
             <Setter Property="ItemContainerStyle">
                 <Setter.Value>
                     <Style TargetType="ComboBoxItem">
                         <Setter Property="Background" Value="#3D3D3D"/>
                         <Setter Property="Foreground" Value="White"/>
                         <Setter Property="BorderThickness" Value="0"/>
                         <Setter Property="Template">
                             <Setter.Value>
                                 <ControlTemplate TargetType="ComboBoxItem">
                                     <Border Name="Border" Padding="4" Background="{TemplateBinding Background}">
                                         <ContentPresenter />
                                     </Border>
                                     <ControlTemplate.Triggers>
                                         <Trigger Property="IsMouseOver" Value="True">
                                             <Setter TargetName="Border" Property="Background" Value="#555"/>
                                         </Trigger>
                                         <Trigger Property="IsSelected" Value="True">
                                              <Setter TargetName="Border" Property="Background" Value="{StaticResource AccentBrush}"/>
                                         </Trigger>
                                     </ControlTemplate.Triggers>
                                 </ControlTemplate>
                             </Setter.Value>
                         </Setter>
                     </Style>
                 </Setter.Value>
             </Setter>
        </Style>

        <!-- ScrollBar Styling -->
        <Style x:Key="ScrollBarThumb" TargetType="{x:Type Thumb}">
            <Setter Property="OverridesDefaultStyle" Value="true"/>
            <Setter Property="IsTabStop" Value="false"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Thumb}">
                        <Border x:Name="rectangle"
                                Background="#555555"
                                CornerRadius="4"
                                BorderThickness="0" />
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter TargetName="rectangle" Property="Background" Value="#888888"/>
                            </Trigger>
                            <Trigger Property="IsDragging" Value="true">
                                <Setter TargetName="rectangle" Property="Background" Value="#AAAAAA"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="{x:Type ScrollBar}">
            <Setter Property="Stylus.IsPressAndHoldEnabled" Value="false"/>
            <Setter Property="Stylus.IsFlicksEnabled" Value="false"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Width" Value="10"/>
            <Setter Property="MinWidth" Value="10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollBar}">
                        <Grid x:Name="Bg" Background="Transparent" SnapsToDevicePixels="true">
                            <Track x:Name="PART_Track" IsDirectionReversed="true">
                                <Track.Thumb>
                                    <Thumb Style="{StaticResource ScrollBarThumb}"/>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- DATA TEMPLATES -->
        
        <!-- Monitor Template -->
        <DataTemplate DataType="{x:Type vm:DashboardItem}">
            <Border Style="{StaticResource MonitorCard}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <!-- Header -->
                    <Grid Grid.Row="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{Binding FriendlyName}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/>
                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                            <Border Background="#F59E0B" CornerRadius="4" Padding="6,2" Margin="0,0,6,0"
                                    Visibility="{Binding IsCalibrating, Converter={StaticResource BoolToVis}}"
                                    ToolTip="A calibration is measuring this display; its gamma, brightness and temperature settings are bypassed until it finishes">
                                <TextBlock Text="CALIBRATING" FontSize="10" FontWeight="Bold" Foreground="#1E1E1E"/>
                            </Border>
                            <Border Background="{Binding BadgeColor}" CornerRadius="4" Padding="6,2">
                                <TextBlock Text="{Binding BadgeText}" FontSize="10" FontWeight="Bold"/>
                            </Border>
                        </StackPanel>
                    </Grid>

                    <!-- Stats (faded while a calibration has this monitor's settings bypassed) -->
                    <StackPanel Grid.Row="1" Margin="0,12,0,0">
                        <StackPanel.Style>
                            <Style TargetType="StackPanel">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsCalibrating}" Value="True">
                                        <Setter Property="Opacity" Value="0.35"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </StackPanel.Style>
                        <TextBlock Text="Gamma Mode:" Foreground="{StaticResource TextDim}" FontSize="11"/>
                        <TextBlock Text="{Binding CurrentGamma}" Margin="0,0,0,8"/>

                        <TextBlock Text="Brightness:" Foreground="{StaticResource TextDim}" FontSize="11"/>
                        <ProgressBar Value="{Binding CurrentBrightness}" Height="4" Margin="0,2,0,4" Background="#444" Foreground="{StaticResource AccentBrush}"/>
                        <TextBlock Text="{Binding CurrentBrightness, StringFormat={}{0}%}" FontSize="12" Margin="0,0,0,8"/>

                        <TextBlock Text="Color Temp:" Foreground="{StaticResource TextDim}" FontSize="11"/>
                        <TextBlock Text="{Binding CurrentTemperatureText}" FontSize="12"/>
                    </StackPanel>

                    <!-- Actions -->
                    <Button Grid.Row="2" Content="Configure Settings"
                            Command="{Binding DataContext.ConfigureMonitorCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                            CommandParameter="{Binding}">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsCalibrating}" Value="True">
                                        <Setter Property="IsEnabled" Value="False"/>
                                        <Setter Property="Opacity" Value="0.5"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>
            </Border>
        </DataTemplate>

        <!-- App Exclusion Template -->
        <DataTemplate DataType="{x:Type vm:AppExclusionItem}">
            <Border Style="{StaticResource MonitorCard}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Text="Night Mode App Auto-Disable" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,8"/>
                    
                    <Grid Grid.Row="1">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <TextBlock Text="Disable Night Mode for:" Foreground="{StaticResource TextDim}" FontSize="12" Margin="0,0,0,8"/>

                        <!-- Add New (ComboBox) -->
                        <Grid Grid.Row="1" Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <ComboBox IsEditable="True" ItemsSource="{Binding RunningApps}"
                                      Text="{Binding NewAppText, UpdateSourceTrigger=PropertyChanged}"
                                      Foreground="White" AllowDrop="False" BorderBrush="{StaticResource CardBorder}">
                                <ComboBox.Resources>
                                    <Style TargetType="TextBox">
                                        <Setter Property="Background" Value="#3D3D3D"/>
                                        <Setter Property="Foreground" Value="White"/>
                                        <Setter Property="CaretBrush" Value="White"/>
                                        <Setter Property="BorderThickness" Value="0"/>
                                    </Style>
                                </ComboBox.Resources>
                            </ComboBox>
                                      
                            <Button Grid.Column="1" Content="Add"
                                    Command="{Binding DataContext.AddExcludedAppCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                    CommandParameter="{Binding}"/>
                        </Grid>

                        <!-- List -->
                        <ListBox Grid.Row="2" ItemsSource="{Binding ExcludedApps}" Background="#333" BorderThickness="0" 
                                 ScrollViewer.VerticalScrollBarVisibility="Auto">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,4" Height="28" Background="Transparent">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <!-- Same FontSize as the checkbox label: mixed sizes center fine but
                                             their baselines drift visibly apart in a shared row -->
                                        <TextBlock Text="{Binding AppName}" VerticalAlignment="Center" Margin="8,0" FontSize="12" Foreground="White" FontWeight="Medium"/>

                                        <!-- Full/Global Checkbox -->
                                        <CheckBox Grid.Column="1" Content="Block All Screens" IsChecked="{Binding FullDisable}"
                                                  VerticalAlignment="Center" Margin="0,0,12,0" Foreground="#AAA" FontSize="12"
                                                  Command="{Binding DataContext.SaveExcludedAppsCommand, RelativeSource={RelativeSource AncestorType=Window}}"/>

                                        <Button Grid.Column="2" Content="✕" Width="24" Height="24"
                                                Padding="0" Background="#4A4A4A" Foreground="#FFF" BorderThickness="0"
                                                Cursor="Hand" VerticalAlignment="Center"
                                                Command="{Binding DataContext.RemoveExcludedAppCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                CommandParameter="{Binding}"/>
                                    </Grid>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Grid>
                </Grid>
            </Border>
        </DataTemplate>
    </Window.Resources>

    <Border Background="#1E1E1E" BorderBrush="#3F3F3F" BorderThickness="1" CornerRadius="8">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Custom Title Bar -->
            <Border Grid.Row="0" Background="#252525" CornerRadius="8,8,0,0" MouseLeftButtonDown="TitleBar_MouseDown">
                <Grid Margin="16,0">
                    <TextBlock Text="Monitor Dashboard" VerticalAlignment="Center" FontWeight="Medium"/>
                    <Button Content="✕" HorizontalAlignment="Right" VerticalAlignment="Center"
                            Background="Transparent" Foreground="#888" BorderThickness="0"
                            Click="Close_Click"/>
                </Grid>
            </Border>

            <!-- Content -->
            <Grid Grid.Row="1" Margin="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <Grid Grid.Row="0" Margin="0,0,0,16">
                    <Grid.ColumnDefinitions>
                         <ColumnDefinition Width="Auto"/>
                         <ColumnDefinition Width="*"/>
                         <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="Active Monitors" FontSize="18" FontWeight="SemiBold" VerticalAlignment="Center"/>
                        <Button Content="Refresh" Margin="16,0,0,0" Command="{Binding RefreshCommand}" Padding="8,4" Background="#444" VerticalAlignment="Center"/>

                    <ToggleButton x:Name="NightModeToggle" Content="Night Mode"
                                  Margin="8,0,0,0" Height="28" Padding="16,0" VerticalAlignment="Center"
                                  IsChecked="{Binding IsNightModeEnabled}"
                                  MouseRightButtonUp="NightModeToggle_RightClick">
                         <ToggleButton.Style>
                             <Style TargetType="ToggleButton">
                                 <Setter Property="Template">
                                     <Setter.Value>
                                         <ControlTemplate TargetType="ToggleButton">
                                             <Border x:Name="border" Background="#444" CornerRadius="4" Padding="{TemplateBinding Padding}">
                                                 <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                             </Border>
                                             <ControlTemplate.Triggers>
                                                 <Trigger Property="IsChecked" Value="True">
                                                     <Setter TargetName="border" Property="Background" Value="{StaticResource AccentBrush}"/>
                                                     <Setter Property="Foreground" Value="White"/>
                                                 </Trigger>
                                             </ControlTemplate.Triggers>
                                         </ControlTemplate>
                                     </Setter.Value>
                                 </Setter>
                                 <Setter Property="Foreground" Value="White"/>
                             </Style>
                         </ToggleButton.Style>
                         <ToggleButton.ContextMenu>
                             <ContextMenu>
                                 <MenuItem Header="Pause for 1 Hour" Command="{Binding Pause1hCommand}"/>
                                 <MenuItem Header="Pause for 4 Hours" Command="{Binding Pause4hCommand}"/>
                                 <MenuItem Header="Pause Until Morning" Command="{Binding PauseUntilMorningCommand}"/>
                             </ContextMenu>
                         </ToggleButton.ContextMenu>
                    </ToggleButton>
                    </StackPanel>
                </Grid>

                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <ItemsControl ItemsSource="{Binding Items}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <!-- ItemTemplate is handled by DataTemplates in Resources -->
                        </ItemsControl>

                        <!-- Night Mode Schedule -->
                        <Expander Header="Night Mode Schedule &amp; Settings" Foreground="White" Margin="0,24,0,0" IsExpanded="True">
                            <StackPanel Margin="12">
                                <TextBlock Text="Configure automatic night mode schedule." Foreground="#888" Margin="0,0,0,12"/>
                                <local:NightModeScheduleControl x:Name="ScheduleEditor"/>
                            </StackPanel>
                        </Expander>
                    </StackPanel>
                </ScrollViewer>
            </Grid>
        </Grid>
    </Border>
</Window>
