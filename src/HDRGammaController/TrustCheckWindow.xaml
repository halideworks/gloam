<Window x:Class="HDRGammaController.TrustCheckWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Trust Check" Width="860" Height="620"
        MinWidth="720" MinHeight="520"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource ThemeBg}"
        Foreground="{DynamicResource ThemeText}"
        SizeChanged="Window_SizeChanged"
        Closed="Window_Closed">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Themes/DarkControls.xaml"/>
                <ResourceDictionary Source="Themes/Brutalist.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid x:Name="BodyRoot" Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal">
            <TextBlock Text="Display" VerticalAlignment="Center" Margin="0,0,10,0"
                       Foreground="{DynamicResource ThemeTextDim}"/>
            <ComboBox x:Name="_monitorPicker" MinWidth="320" VerticalAlignment="Center"
                      SelectionChanged="Monitor_SelectionChanged"/>
        </StackPanel>

        <Border Grid.Row="1" Margin="0,10,0,2" Padding="12,10"
                Background="{DynamicResource ThemeSurface}"
                BorderBrush="{DynamicResource ThemeBorder}" BorderThickness="1">
            <TextBlock x:Name="_latest" FontSize="13" TextWrapping="Wrap"
                       Foreground="{DynamicResource ThemeText}"/>
        </Border>

        <TextBlock x:Name="_status" Grid.Row="2" FontSize="12" Margin="0,6,0,0"
                   TextWrapping="Wrap" Foreground="{DynamicResource ThemeTextDim}"/>

        <Grid Grid.Row="3" Margin="0,4,0,8">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Canvas x:Name="_deltaECanvas" Margin="0,4" MinHeight="140" ClipToBounds="True"
                    Background="{DynamicResource ThemeSurface}"/>
            <Canvas x:Name="_duvCanvas" Grid.Row="1" Margin="0,4" MinHeight="140" ClipToBounds="True"
                    Background="{DynamicResource ThemeSurface}"/>
        </Grid>

        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <CheckBox x:Name="_reminderToggle" Content="Remind me monthly to run a trust check"
                      VerticalAlignment="Center" Foreground="{DynamicResource ThemeTextDim}"
                      Checked="Reminder_Checked" Unchecked="Reminder_Unchecked"/>
            <Button x:Name="_runButton" Grid.Column="1" Content="Run Check" Margin="8,0,0,0"
                    Style="{DynamicResource BrutBtnPrimary}" Click="Run_Click"/>
            <Button Grid.Column="2" Content="Export CSV…" Margin="8,0,0,0"
                    Style="{DynamicResource BrutBtnSecondary}" Click="Export_Click"/>
            <Button Grid.Column="3" Content="Close" Margin="8,0,0,0"
                    Style="{DynamicResource BrutBtnSecondary}" Click="Close_Click"/>
        </Grid>
    </Grid>
</Window>
