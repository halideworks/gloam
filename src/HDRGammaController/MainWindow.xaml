<Window x:Class="HDRGammaController.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:tb="http://www.hardcodet.net/taskbar"
        xmlns:vm="clr-namespace:HDRGammaController.ViewModels"
        Title="Gloam" Height="0" Width="0"
        WindowStyle="None"
        ShowInTaskbar="False"
        Visibility="Hidden">

    <Window.Resources>
        <!-- Brutalist tray menu: hard square border, red highlight, follows the app palette -->
        <Style x:Key="ThemedContextMenu" TargetType="ContextMenu">
            <Setter Property="Background" Value="{DynamicResource MenuBackground}"/>
            <Setter Property="Foreground" Value="{DynamicResource MenuForeground}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource MenuBorder}"/>
            <Setter Property="FontFamily" Value="{DynamicResource BodyFont}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="3"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ContextMenu">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Cycle"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Minimal MenuItem Template without icon column -->
        <ControlTemplate x:Key="SimpleMenuItemTemplate" TargetType="MenuItem">
            <Border x:Name="Border" Background="Transparent" BorderThickness="0" Padding="11,7">
                <ContentPresenter x:Name="cp" ContentSource="Header"
                                  HorizontalAlignment="Left" VerticalAlignment="Center"/>
            </Border>
            <ControlTemplate.Triggers>
                <Trigger Property="IsHighlighted" Value="True">
                    <Setter TargetName="Border" Property="Background" Value="{DynamicResource ThemeAccent}"/>
                    <Setter TargetName="cp" Property="TextElement.Foreground" Value="{DynamicResource ThemeOnAccent}"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.5"/>
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>

        <!-- Submenu MenuItem Template (has arrow) -->
        <ControlTemplate x:Key="SubMenuItemTemplate" TargetType="MenuItem">
            <Border x:Name="Border" Background="Transparent" BorderThickness="0" Padding="11,7">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <ContentPresenter x:Name="cp" Grid.Column="0" ContentSource="Header"
                                      HorizontalAlignment="Left" VerticalAlignment="Center"/>
                    <Path Grid.Column="1" x:Name="Arrow"
                          Data="M0,0 L4,4 L0,8 Z"
                          Fill="{DynamicResource MenuForeground}"
                          VerticalAlignment="Center" Margin="8,0,0,0" Visibility="Collapsed"/>
                    <Popup x:Name="PART_Popup"
                           IsOpen="{Binding IsSubmenuOpen, RelativeSource={RelativeSource TemplatedParent}}"
                           Placement="Right" AllowsTransparency="True" Focusable="False" PopupAnimation="None">
                        <Border Background="{DynamicResource MenuBackground}"
                                BorderBrush="{DynamicResource MenuBorder}"
                                BorderThickness="0" Padding="3">
                            <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Cycle"/>
                        </Border>
                    </Popup>
                </Grid>
            </Border>
            <ControlTemplate.Triggers>
                <Trigger Property="IsHighlighted" Value="True">
                    <Setter TargetName="Border" Property="Background" Value="{DynamicResource ThemeAccent}"/>
                    <Setter TargetName="cp" Property="TextElement.Foreground" Value="{DynamicResource ThemeOnAccent}"/>
                    <Setter TargetName="Arrow" Property="Fill" Value="{DynamicResource ThemeOnAccent}"/>
                </Trigger>
                <Trigger Property="HasItems" Value="True">
                    <Setter TargetName="Arrow" Property="Visibility" Value="Visible"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.5"/>
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>

        <Style x:Key="ThemedMenuItem" TargetType="MenuItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource MenuForeground}"/>
            <Setter Property="FontFamily" Value="{DynamicResource BodyFont}"/>
            <Setter Property="Header" Value="{Binding Header}" />
            <Setter Property="Command" Value="{Binding Command}" />
            <Setter Property="ItemsSource" Value="{Binding SubItems}" />
            <!-- Top-level toggles (Start with Windows, Refresh) keep the menu open;
                 items without the property (MonitorViewModel) bind to default false. -->
            <Setter Property="StaysOpenOnClick" Value="{Binding StaysOpenOnClick}" />
            <Setter Property="Template" Value="{StaticResource SubMenuItemTemplate}"/>
        </Style>

        <Style x:Key="ThemedSubMenuItem" TargetType="MenuItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource MenuForeground}"/>
            <Setter Property="FontFamily" Value="{DynamicResource BodyFont}"/>
            <Setter Property="Header" Value="{Binding Header}" />
            <Setter Property="Command" Value="{Binding Command}" />
            <Setter Property="StaysOpenOnClick" Value="{Binding StaysOpenOnClick}" />
            <Setter Property="Template" Value="{StaticResource SimpleMenuItemTemplate}"/>
        </Style>
    </Window.Resources>

    <Grid>
        <tb:TaskbarIcon x:Name="MyNotifyIcon"
                        ToolTipText="{Binding TrayToolTipText}"
                        IconSource="/app.ico"
                        MenuActivation="RightClick">
            <tb:TaskbarIcon.ContextMenu>
                <ContextMenu ItemsSource="{Binding TrayItems}"
                             Style="{StaticResource ThemedContextMenu}">
                    <ContextMenu.ItemContainerStyle>
                        <Style TargetType="MenuItem" BasedOn="{StaticResource ThemedMenuItem}">
                            <Setter Property="ItemContainerStyle" Value="{StaticResource ThemedSubMenuItem}"/>
                        </Style>
                    </ContextMenu.ItemContainerStyle>
                </ContextMenu>
            </tb:TaskbarIcon.ContextMenu>
        </tb:TaskbarIcon>
    </Grid>
</Window>
